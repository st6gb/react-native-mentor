import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    borderColor: "black",
    borderWidth: 0.5,
    flex: 1,
    justifyContent: "flex-start"
  },
  header: {}
});